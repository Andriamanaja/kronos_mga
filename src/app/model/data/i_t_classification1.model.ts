import { base_data } from './base_data.model';

export class i_t_classification1 extends base_data {

    constructor( create_uid : number=0, name : string="", write_uid : number = 0, id? : number){
        super(create_uid, name, write_uid, id);
    }

}