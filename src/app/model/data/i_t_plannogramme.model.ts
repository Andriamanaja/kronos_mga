import { base_data } from './base_data.model';

export class i_t_plannogramme extends base_data {

    constructor(create_uid : number, name : string, write_uid : number, id? : number){
        super(create_uid, name, write_uid, id);
    }
    
}