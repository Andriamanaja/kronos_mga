import { FieldType } from '@ngx-formly/core';
export declare class FormlyFieldCheckbox extends FieldType {
    defaultOptions: {
        templateOptions: {
            hideLabel: boolean;
        };
    };
}
