import { TkIfScreenSizeDirective } from './screen-size.directive';

describe('ScreenSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new TkIfScreenSizeDirective();
    expect(directive).toBeTruthy();
  });
});
