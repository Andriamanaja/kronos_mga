import { ElementSizeDirective } from './element-size.directive';

describe('ElementSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ElementSizeDirective();
    expect(directive).toBeTruthy();
  });
});
