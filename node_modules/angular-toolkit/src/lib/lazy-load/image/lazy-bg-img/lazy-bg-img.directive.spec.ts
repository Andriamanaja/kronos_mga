import { TkLazyBgImgDirective } from './lazy-bg-img.directive';

describe('LazyBgImgDirective', () => {
  it('should create an instance', () => {
    const directive = new TkLazyBgImgDirective();
    expect(directive).toBeTruthy();
  });
});
